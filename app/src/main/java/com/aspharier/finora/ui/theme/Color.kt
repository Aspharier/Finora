package com.aspharier.finora.ui.theme

import androidx.compose.ui.graphics.Color


val NeonGreen = Color(0xFFACF532)   // Primary CTA, highlights
val PureWhite = Color(0xFFFFFFFF)   // Primary text
val DarkSurface = Color(0xFF3E3E3E)  // Cards, containers (dark theme)
val OceanGreen = Color(0xFF107459)  // Secondary accents
val MintGreen = Color(0xFFABDAC5)   // Tertiary highlights

// Semantic colors
val SuccessGreen = Color(0xFF4CAF50)
val ErrorRed = Color(0xFFE53935)

// Material 3 Expressive Navigation Colors
val ExpressiveOrange = Color(0xFFD4714A)   // Selected item background
val ExpressiveBrown = Color(0xFF5C4033)    // Container background
val ExpressiveIconDark = Color(0xFF3D2A1E) // Icon color on selected

// Light Theme Colors
val LightBackground = Color(0xFFF5F5F5)    // Light mode background
val LightSurface = Color(0xFFFFFFFF)       // Light mode cards/containers
val LightSurfaceVariant = Color(0xFFE8E8E8) // Light mode surface variant
val DarkText = Color(0xFF1A1A1A)           // Primary text for light mode
val DarkTextSecondary = Color(0xFF5A5A5A)  // Secondary text for light mode

// Dark Theme Colors
val DarkBackground = Color(0xFF121212)     // Dark mode background
val DarkSurfaceVariant = Color(0xFF2A2A2A) // Dark mode surface variant

// Glass Effect Colors - Light Mode
val GlassWhite = Color(0x33FFFFFF)         // Semi-transparent white for glass effect
val GlassBorder = Color(0x1AFFFFFF)        // Subtle border for glass effect

// Glass Effect Colors - Dark Mode (higher visibility)
val GlassWhiteDark = Color(0x40FFFFFF)     // More visible semi-transparent white for dark mode
val GlassBorderDark = Color(0x33FFFFFF)    // More visible border for dark mode

// Numpad Colors - Dark Mode
val NumpadBackgroundDark = Color(0xFF2A2A2A)  // Solid dark background for numpad keys
val NumpadBorderDark = Color(0xFF404040)      // Visible border for numpad keys in dark mode

// Numpad Colors - Light Mode  
val NumpadBackgroundLight = Color(0xFFF0F0F0) // Light gray background for numpad keys
val NumpadBorderLight = Color(0xFFD0D0D0)     // Border for numpad keys in light mode